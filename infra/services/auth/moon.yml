project:
  name: app-auth

dependsOn:
  - nats-go
  - utils-go
  - db

tasks:
  build:
    command: go build -o build/auth main.go
    inputs:
      - ./**/*.go
  run:
    command: go run main.go

  start:
    command: ./build/auth
